#set( $symbol_pound = '#' )
#set( $symbol_dollar = '$' )
#set( $symbol_escape = '\' )
This folder is to host the rate limiter related projects.

The following projects are needed for the RateLimiterServiceImpl/RateLimiterServiceConsumer development:

* All the SOA runtime projects (such as BindingFwk, SOAClient,SOAServer, SOATools)
* RateLimiterService project
* SecurityFramework project

